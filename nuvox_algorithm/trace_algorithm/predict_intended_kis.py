from typing import Dict, List


def predict_intended_kis(trace: List[Dict[str, float]]) -> Dict[str, float]:
    """Returns dictionary mapping key-id-sequence to predicted
    probability that this was the intended KIS."""
    pass
